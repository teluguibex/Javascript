// includes : It return the result into the boolean format.
            //If value is present/available the it return true.
            // If value is not present/available then it return false.

let arr = [10,20,30,40,50,60,70];

if(arr.includes(500)) {
    console.log("It existed");
}else {
    console.log("It is not existed");
}

